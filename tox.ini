[tox]
envlist = py310, py311

[gh-actions]
python = 
    3.11: py311

[testenv]
setenv =
    PYTHONPATH = {toxinidir}/src
deps = 
    pytest
    pytest-cov
    -r requirements.txt
commands =
    pytest --cov=blinker --cov-report=term-missing